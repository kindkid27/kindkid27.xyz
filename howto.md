this is for my dumb ass since I forget shit
on the home server, connect the vscode SSH, I'd prefer if you use the local 192.168.0.60 IP, but you can use the public IP (wireguard) if you want slow ass speeds
to do the live reload thing so you see your changes live
run this `bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload`
I'm not even gonna bother going through building it since it should already be working on the home server